Name: Build

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main, develop]

permissions:
  contents: write # ضروري للسماح للذكاء الاصطناعي بإنشاء ملفات جديدة
  checks: write
  actions: read

jobs:
  build:
    name: 🏗️ Build Application
    runs-on: ubuntu-latest

    env:
      NODE_ENV: production
      NEXT_PUBLIC_BASE_URL: https://tec-ecosystem.vercel.app
      DATABASE_URL: postgresql://dummy:user@dummy:5432/dummy?schema=public
      NEXT_PUBLIC_PI_APP_ID: dummy_app_id
      NEXT_PUBLIC_PI_SANDBOX: true
      NEXT_PUBLIC_API_URL: http://localhost:3000
      SKIP_ENV_VALIDATION: true
      # ربط المفاتيح والنماذج المحدثة من الـ Secrets
      OPENROUTER_API_KEY: ${{ secrets.OPENROUTER_API_KEY }} 
      CLAUDE_MODEL: ${{ secrets.CLAUDE_MODEL }}
      GEMINI_MODEL: ${{ secrets.GEMINI_MODEL }}

    steps:
      - name: 📥 Checkout code
        uses: actions/checkout@v4

      - name: 📦 Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "npm"

      - name: 📚 Install dependencies
        run: npm ci

      - name: 🔧 Generate Prisma Client
        run: npx prisma generate

      - name: 🏗️ Build Application
        run: npm run build

      # تشغيل السكربت الذكي مع نظام الاختبار الذاتي ومنع البجات
      - name: 🤖 Run AI Dual-Model Debugger
        id: ai_debugger
        run: node index.js
        continue-on-error: true # لضمان إرسال التنبيه في الخطوة القادمة إذا فشل الاختبار

      # نظام التنبيهات الفوري في حال فشل الاختبار الذاتي للكود
      - name: ⚠️ AI Safety Alert
        if: steps.ai_debugger.outcome == 'failure'
        run: |
          echo "### ❌ فشل الاختبار الذاتي (Auto-Testing Failure)!" >> $GITHUB_STEP_SUMMARY
          echo "الذكاء الاصطناعي حاول إنشاء كود جديد ولكنه يحتوي على أخطاء (Bugs) تمنع تشغيله." >> $GITHUB_STEP_SUMMARY
          echo "تم منع إنشاء الملفات الجديدة لحماية استقرار المشروع." >> $GITHUB_STEP_SUMMARY
          exit 1

      - name: 📦 Upload Secure Artifacts
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: ai-verified-code
          path: |
            generated-*.js
            .next/
          retention-days: 7

      - name: ✅ Build Success
        if: steps.ai_debugger.outcome == 'success'
        run: echo "✅ تم البناء بنجاح واجتاز الكود المولد جميع اختبارات السلامة!"

Name: Build

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main, develop]

permissions:
  contents: write 
  checks: write
  actions: read

jobs:
  build:
    name: 🏗️ Build & AI Secure Dev
    runs-on: ubuntu-latest

    env:
      NODE_ENV: production
      OPENROUTER_API_KEY: ${{ secrets.OPENROUTER_API_KEY }} 
      CLAUDE_MODEL: ${{ secrets.CLAUDE_MODEL }}
      GEMINI_MODEL: ${{ secrets.GEMINI_MODEL }}

    steps:
      - name: 📥 Checkout code
        uses: actions/checkout@v4

      - name: 📦 Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "npm"

      - name: 📚 Install dependencies
        run: npm ci

      - name: 🤖 Run AI Self-Healing System
        id: ai_process
        run: node index.js
        continue-on-error: true 

      - name: ⚠️ AI Failure Summary
        if: steps.ai_process.outcome == 'failure'
        run: |
          echo "### ❌ فشل النظام الذكي في الوصول لكود مستقر" >> $GITHUB_STEP_SUMMARY
          echo "تم إجراء 3 محاولات تصحيح تلقائي ولم ينجح الاختبار البرمجي." >> $GITHUB_STEP_SUMMARY
          echo "يرجى مراجعة الـ Logs لإصلاح المشكلة يدوياً." >> $GITHUB_STEP_SUMMARY
          exit 1

      - name: 📦 Upload Final Deliverables
        if: success()
        uses: actions/upload-artifact@v4
        with:
          name: ai-verified-package
          path: |
            final-service.js
            DOCUMENTATION.md
          retention-days: 7

      - name: ✅ Build Success
        if: success()
        run: echo "✅ تم إنشاء الكود وتوثيقه بنجاح!"

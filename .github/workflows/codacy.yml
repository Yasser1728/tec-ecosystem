name: "فحص الأمان عبر كوداسي - Codacy Security Scan"

on:
  push:
    branches: ["main", "develop"]
  pull_request:
    branches: ["main"]

permissions:
  contents: read
  security-events: write  # دي الصلاحية اللي كانت ناقصة عشان يرفع النتائج
  actions: read

jobs:
  codacy-security-scan:
    name: "بدء عملية فحص الحوكمة والأمان"
    runs-on: ubuntu-latest

    steps:
      - name: "📥 جلب الكود المصدري"
        uses: actions/checkout@v4

      - name: "🛡️ تشغيل محرك فحص كوداسي"
        uses: codacy/codacy-analysis-cli-action@v4
        with:
          project-token: ${{ secrets.CODACY_PROJECT_TOKEN }}
          verbose: true
          output: results.sarif
          format: sarif
          directory: "/github/workspace"

      - name: "🔍 فحص نتائج الأمان"
        id: check-sarif
        run: |
          if [ -f results.sarif ]; then
            echo "✅ ملف النتائج موجود"
            SARIF_SIZE=$(stat -f%z results.sarif 2>/dev/null || \
              stat -c%s results.sarif 2>/dev/null)
            echo "📊 حجم الملف: $SARIF_SIZE بايت"
            
            # Check if SARIF has valid runs with results
            RUNS_COUNT=$(jq -r '.runs | length' results.sarif 2>/dev/null || echo "0")
            echo "📊 عدد الـ runs: $RUNS_COUNT"
            
            if [ "$RUNS_COUNT" -gt 0 ]; then
              # Check if at least one run has results
              HAS_RESULTS=$(jq -r '.runs[] | select(.results != null and (.results | length) > 0) | .results | length' results.sarif 2>/dev/null | head -1 || echo "0")
              if [ "$HAS_RESULTS" -gt 0 ]; then
                echo "has_results=true" >> $GITHUB_OUTPUT
                echo "✅ الملف يحتوي على نتائج صالحة"
              else
                echo "has_results=false" >> $GITHUB_OUTPUT
                echo "⚠️ الملف لا يحتوي على نتائج"
              fi
            else
              echo "has_results=false" >> $GITHUB_OUTPUT
              echo "⚠️ الملف لا يحتوي على runs صالحة"
            fi
          else
            echo "has_results=false" >> $GITHUB_OUTPUT
            echo "❌ ملف النتائج غير موجود"
          fi

      - name: "📊 رفع تقرير الأمان إلى جيت هاب"
        # تم التحديث إلى v4 - v3 سيتم إيقافه في ديسمبر 2026
        # نرفع فقط إذا كان الملف يحتوي على نتائج صالحة
        if: steps.check-sarif.outputs.has_results == 'true'
        uses: github/codeql-action/upload-sarif@v4
        # نتابع حتى لو فشل الرفع
        continue-on-error: true
        with:
          sarif_file: results.sarif
          # إضافة الـ token لضمان نجاح الرفع
          token: ${{ github.token }}

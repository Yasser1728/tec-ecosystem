# 🛠️ Scripts Directory

هذا الدليل يحتوي على scripts مفيدة لإدارة مشروع TEC.

---

## 📜 القائمة الكاملة للـ Scripts

### 1. `check-vercel-config.sh` ✅ (جديد)

**الغرض:** فحص شامل لجميع تكوينات وملفات Vercel

**الاستخدام:**

```bash
# طريقة 1: مباشرة
bash scripts/check-vercel-config.sh

# طريقة 2: عبر npm
npm run vercel:check

# طريقة 3: فحص + بناء
npm run vercel:validate
```

**ماذا يفحص:**

- ✅ وجود الملفات الأساسية (vercel.json, .vercelignore, etc.)
- ✅ أذونات vercel-ignore.sh
- ✅ اختبار منطق vercel-ignore.sh للفروع المختلفة
- ✅ صلاحية JSON في vercel.json
- ✅ package.json scripts (build, dev, start, postinstall)
- ✅ تكوين next.config.js
- ✅ متغيرات البيئة في .env.example
- ✅ GitHub Actions workflows

**النتيجة:**

```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
📊 النتائج:
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
✓ نجح: 29
⚠ تحذيرات: 0
✗ أخطاء: 0
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
🎉 جميع فحوصات Vercel نجحت!
✅ المشروع جاهز للنشر على Vercel
```

---

### 2. `deploy.sh`

**الغرض:** نشر المشروع على Vercel

**الاستخدام:**

```bash
bash scripts/deploy.sh
```

---

### 3. `setup.sh`

**الغرض:** إعداد المشروع للمرة الأولى

**الاستخدام:**

```bash
bash scripts/setup.sh
```

**ماذا يفعل:**
- يثبت التبعيات
- يولد Prisma Client
- يبني المشروع

---

### 4. `generate-domains.js`

**الغرض:** توليد هياكل domains جديدة

**الاستخدام:**

```bash
node scripts/generate-domains.js
```

---

## 🚀 Quick Start

### قبل النشر على Vercel:

```bash
# 1. فحص التكوينات
npm run vercel:check

# 2. بناء المشروع
npm run build

# 3. اختبار محلياً
npm start
```

### حل المشاكل:

```bash
# إذا فشل vercel-ignore.sh
chmod +x vercel-ignore.sh
chmod +x scripts/check-vercel-config.sh

# إذا كانت الأذونات غير صحيحة
find scripts -name "*.sh" -exec chmod +x {} \;
```

---

## 📝 إضافة Scripts جديدة

عند إضافة script جديد:

1. **ضعه في `/scripts`**
2. **اجعله قابلاً للتنفيذ:**
   ```bash
   chmod +x scripts/your-script.sh
   ```
3. **أضف توثيق له هنا**
4. **أضف npm script في package.json إذا لزم الأمر:**
   ```json
   "scripts": {
     "your-command": "bash scripts/your-script.sh"
   }
   ```

---

## 🔧 Maintenance

### تحديث Scripts:

```bash
# عرض جميع Scripts
ls -lah scripts/

# إصلاح الأذونات
chmod +x scripts/*.sh

# اختبار جميع Scripts
npm run vercel:check
npm run build
```

---

**آخر تحديث:** 23 يناير 2026  
**المسؤول:** TEC Sovereign Agent

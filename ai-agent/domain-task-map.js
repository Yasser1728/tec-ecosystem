import { ledger } from './core/ledger.js'
import { openRouterRequest } from './core/openrouter.js'

// Domain task map for the AI agent. Tasks are grouped per domain and can
// reference core utilities (ledger, openrouter, etc.)

export const domainTaskMap = {
  // Example domains/tasks. Extend as needed.
}

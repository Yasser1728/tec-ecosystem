# REMOVED: env.local should not be committed
# This repository now uses env.local.example for environment variables.
# Do NOT store secrets here. To untrack this file locally run:
#   git rm --cached env.local
#   git commit -m "chore: remove env.local from repo and keep example"

NODE_ENV="development"

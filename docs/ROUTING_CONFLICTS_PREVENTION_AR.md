# حل تعارضات التوجيه في Next.js

## المشكلة

حدث خطأ في بناء Vercel لمستودع Tec-App:

```
⨯ Conflicting app and page file was found, please remove the conflicting files to continue:
⨯   "pages/index.tsx" - "app/page.tsx"
```

يحدث هذا الخطأ عندما يكون لديك نفس المسار في كل من:
- دليل `pages/` (Pages Router التقليدي)
- دليل `app/` (App Router الجديد)

Next.js لا يسمح بوجود تعارضات في المسارات بين النظامين.

## الحل المُطبق

### 1. فحص تلقائي للتعارضات

أنشأنا نظام فحص تلقائي يعمل قبل كل عملية بناء:

```bash
npm run check:routes
```

### 2. منع التعارضات في مرحلة البناء

تم إضافة `prebuild` hook في `package.json`:

```json
{
  "scripts": {
    "prebuild": "node scripts/check-routing-conflicts.js"
  }
}
```

الآن، **قبل كل عملية بناء**، سيتم:
- ✅ فحص جميع الملفات في `pages/` و `app/`
- ✅ الكشف عن أي تعارضات في المسارات
- ❌ إيقاف البناء إذا وجدت تعارضات
- ✅ السماح بالبناء إذا لم توجد تعارضات

### 3. السكريبت الخاص بالفحص

موقع السكريبت:
```
scripts/check-routing-conflicts.js
```

يقوم بـ:
1. مسح جميع الملفات في `pages/`
2. مسح جميع الملفات في `app/`
3. تحويل مسارات الملفات إلى مسارات الـ routes
4. مقارنة المسارات للعثور على التكرار
5. الإبلاغ عن أي تعارضات

## الحالة الحالية لمستودع tec-ecosystem

✅ **لا توجد تعارضات في التوجيه**

البنية الهجينة المستخدمة:
- Pages Router: 175 مسار (التطبيق الرئيسي)
- App Router: 1 مسار (`/validation-key.txt`)
- التعارضات: 0

## أمثلة على التعارضات

### مثال على تعارض ❌

```
pages/index.tsx    →  المسار: /
app/page.tsx       →  المسار: /
```
**النتيجة**: خطأ في البناء! ❌

### مثال على عدم تعارض ✅

```
pages/index.js           →  المسار: /
app/validation-key.txt/route.js  →  المسار: /validation-key.txt
```
**النتيجة**: البناء ناجح! ✅

## كيفية حل التعارضات

إذا وجد الفاحص تعارضات، لديك خياران:

### الخيار 1: حذف من Pages Router
احذف أو انقل الملف من دليل `pages/`

### الخيار 2: حذف من App Router
احذف أو انقل الملف من دليل `app/`

## أفضل الممارسات

1. **قرر على نظام التوجيه الأساسي**: للميزات الجديدة، قرر ما إذا كنت تريد استخدام Pages أو App Router
2. **افصل API Routes**: مسارات API في `pages/api/` لا تتعارض مع App Router
3. **وثق الاستخدام الهجين**: إذا كنت تستخدم كلا النظامين، وثق أي المسارات تستخدم أي نظام
4. **شغل الفحوصات محلياً**: قبل الـ commit، شغل `npm run check:routes`
5. **تكامل CI/CD**: الـ pre-build hook يضمن اكتشاف التعارضات في CI/CD

## الوثائق

لمزيد من التفاصيل، راجع:
- `docs/ROUTING_CONFLICTS_PREVENTION.md` (الإنجليزية)
- `scripts/check-routing-conflicts.js` (الكود)

## الخلاصة

✅ **تم حل المشكلة**
- أنشأنا نظام فحص تلقائي
- أضفنا pre-build hook لمنع التعارضات
- وثقنا البنية الهجينة المستخدمة
- اختبرنا أن الفحص يعمل بشكل صحيح
- البناء يعمل بنجاح بدون تعارضات

🎉 **لن تحدث هذه المشكلة مرة أخرى!**
